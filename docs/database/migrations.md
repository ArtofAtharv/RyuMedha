# Migrations

Database definition relies primarily on consolidated SQL scripts like `ULTIMATE_CONSOLIDATED_SCHEMA.sql`. No strict migration tool (like Prisma migrate or Supabase migrations folder) seems strictly enforced based on the root level files, though `supabase/whatsapp_tracking_migration.sql` suggests point-in-time schema changes are manually tracked.
