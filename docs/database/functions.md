# Functions

- `get_profile_id_from_jwt()`: Extracts the phone number from the JWT `sub` claim and looks up the `profile_id`.
- `update_modified_column()`: Generic trigger function to update `updated_at` timestamps.
