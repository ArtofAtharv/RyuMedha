# Glossary

- **RLS (Row Level Security)**: PostgreSQL feature used to restrict data access based on the user's identity.
- **OTP (One-Time Password)**: A code used to authenticate users on the web dashboard.
- **Supabase Edge Functions**: Serverless functions hosted on Supabase, used for background tasks and webhook processing.
